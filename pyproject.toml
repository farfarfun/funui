[project]
name = "funui"
version = "1.0.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=7.0",
]
demo = [
    "fastapi>=0.141.1",
    "nicegui>=3.16.0",
    "starlette>=1.6.0",
    "uvicorn>=0.32.0",
]
